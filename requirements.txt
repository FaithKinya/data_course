# Core — Phases 1-3, 7-8
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
duckdb>=0.9.0
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0

# Data quality — Phase 8
great-expectations>=0.18.0

# Streaming — Phase 6
kafka-python>=2.0.2

# Orchestration helpers — Phase 4 (Airflow runs in Docker)
apache-airflow-client>=2.7.0

# dbt — Phase 5 (install dbt separately: pip install dbt-duckdb)
# dbt-duckdb>=1.7.0

# Dev / notebooks
jupyter>=1.0.0
ipykernel>=6.25.0

# Testing
pytest>=7.4.0
